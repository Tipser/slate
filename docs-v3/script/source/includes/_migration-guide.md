# Migration guide

## Initialization

The way to initialize Tipser Script has changed. It used to be:

```js
tipserScript.initialize(posId,config)
```

It is now:

```js
tipserScript.initialize(posId, config)
```
